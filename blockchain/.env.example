# Hardhat 3 Configuration Variables
# Don't use .env files - use Hardhat's secure keystore instead!

# To set configuration variables, run:
# npx hardhat keystore set --dev SEPOLIA_PRIVATE_KEY
# npx hardhat keystore set --dev SEPOLIA_RPC_URL

# Required variables:
# SEPOLIA_PRIVATE_KEY - Your wallet private key (generate with: node -e "const ethers = require('ethers'); const wallet = ethers.Wallet.createRandom(); console.log('Address:', wallet.address); console.log('Private Key:', wallet.privateKey);")
# SEPOLIA_RPC_URL - Sepolia RPC endpoint (e.g., https://ethereum-sepolia-rpc.publicnode.com)